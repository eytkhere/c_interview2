#include <stdio.h>
#include <stdlib.h>

int main(){
    int x, y;
    printf("Enter number:\n");
    scanf("%d", &x);
    printf("Enter which bit to toggle:\n");
    scanf("%d", &y);
    int new_num=(x^(1<<(y-1)));
    printf("%d", new_num);
}
